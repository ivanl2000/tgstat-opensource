# Telegram User API — получить на https://my.telegram.org
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_PHONE=+79001234567

# База данных (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///data/tgstat.db

# Веб-сервер
HOST=0.0.0.0
PORT=8080

# Токен для POST /api/collect (обязателен; без него эндпоинт отключён)
# Заголовок: X-API-Token: <токен>  или  Authorization: Bearer <токен>
COLLECT_API_TOKEN=замените_на_случайный_секрет
